//! DecodeBackend implementation for Vulkan.
//! All methods use defaults returning None — decode pipeline not yet ported.

use crate::backend::DecodeBackend;
use crate::vulkan::VulkanBackend;

impl DecodeBackend for VulkanBackend {}
